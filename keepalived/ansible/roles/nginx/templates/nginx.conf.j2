events {}

http {
    server {
        listen 80;
        server_name _;

        location = /health {
            access_log off;
            return 200 "OK\n";
        }

        location / {
            root /var/www/html;
            index index.html;
        }
    }
}